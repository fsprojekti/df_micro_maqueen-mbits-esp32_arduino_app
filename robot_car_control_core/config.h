// config.h
#ifndef CONFIG_H
#define CONFIG_H

extern const char* ssid;      // Declare ssid as an external variable
extern const char* password;  // Declare password as an external variable
extern char studentAppIP[16];      // Declare studentAppIP as an external variable
extern const char* endpoint;  // Declare endpoint as an external variable

#endif